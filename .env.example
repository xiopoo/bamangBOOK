# Production origin used for canonical URLs, sitemap and social metadata.
NEXT_PUBLIC_SITE_URL=https://example.com

# Optional image service credentials used by existing project scripts.
IMA_CLIENT_ID=
IMA_API_KEY=
